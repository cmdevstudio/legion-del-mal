name: Reclutamiento de Agente

on: push

jobs:
  reporte-de-reclutamiento:
    runs-on: ubuntu-latest
    steps:
      - name: Agente se presenta
        run: echo "Hola soy Lex Luthor, y el lider de a legion del mal"

      - name: Reporte de identidad
        run: |
          echo "Reporte de identidad"
          echo "===================="
          echo ""
          echo "Nombre: ${{ github.actor }}"
          echo ""
          echo "Contexto de HitHub"
          echo "Repo: ${{ github.repository }}"
          echo "Operación: ${{ github.sha }}"
          echo "Metadata: ${{ toJson(github) }}"

      - name: Capacidades del Agente
        run: |
          echo "Capacidades detectadas:"
          echo "Sistema Operativo"
          uname -a 
          echo "Habilidades técnicas"
          node --version
          git --version
          phyton3 --version

      -name
        run: |
          echo "Reconocmiento del terreno"
          ls /